/**
 * main.js
 * 
 * Application entry point for Poli-Granulator.
 * This is a thin wrapper that imports the boot module to initialize
 * the entire application. Keeping this file minimal ensures fast
 * initial parsing and allows the boot process to handle all setup.
 */

import './app/boot.js';
